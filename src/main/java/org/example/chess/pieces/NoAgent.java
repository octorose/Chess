package org.example.chess.pieces;

import org.example.Position;

public class NoAgent extends ChessAgent{

    public NoAgent(Position position, String color, boolean isAlive, int value, String label) {
        super(position, color, isAlive, value, label);
    }

}
